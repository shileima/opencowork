import type { SSOLoginConfig } from '../types';
import type SSOLogger from '../utils/logger';
export default class SSOStorage {
    private logger;
    private cache;
    constructor(logger: SSOLogger);
    /**
     * 保存state值
     * @param state state值
     */
    setState(state: string, keySuffix: string): void;
    /**
     * !!!仅限cli模式
     * 读取state值
     * @returns state值
     */
    getState(): string | null;
    /**
     * 保存PKCE code_verifier值
     * @param verifier PKCE verifier值
     */
    setVerifier(verifier: string, keySuffix: string): void;
    /**
     * !!!仅限cli模式
     * 读取PKCE code_verifier值
     * @returns PKCE verifier值
     */
    getVerifier(): string | null;
    /**
     * 保存SSO web config
     * @param configs SSO web config
     */
    setConfigs(configs: SSOLoginConfig, keySuffix: string): void;
    /**
     * 保存key、value
     * @param key key值
     * @param value value值
     */
    private setStorage;
    /**
     * 读取key对应的value值
     * @param key key值
     * @returns value值
     */
    private getStorage;
}
