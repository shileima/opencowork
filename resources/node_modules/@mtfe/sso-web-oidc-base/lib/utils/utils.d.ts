import { type SSOErrorCode, SSOAccessEnvType } from '../config/const';
import type SSOStorage from '../store/storage';
import type { SSOLoginConfig, SSOReturnType } from '../types';
/**
 * 获取固定查询参数对象（不要随便添加参数，甚至包括登录参数也不要随便添加）
 * @param moreParams
 * @returns 参数对象
 */
export declare const getFixedSearchParams: ({ sdkVersion, locale, mode, }: {
    sdkVersion: string;
} & SSOLoginConfig) => Record<string, string | number>;
/**
 * 获取实际使用域名
 * @param originMap 域名映射
 * @param configs 登录配置
 * @returns 域名
 */
export declare const getRealOrigin: (originMap: Record<string, string>, configs: SSOLoginConfig) => string;
/**
 * 获取返回结构体
 * @param code 错误码
 * @param data 其他数据
 * @param msg 错误消息
 * @returns 返回结构体
 */
export declare const buildResult: <T = unknown>(code: SSOErrorCode, data: T, msg?: string) => SSOReturnType<T>;
/**
 * 生成state参数
 * @returns state值
 */
export declare function generateState(): string;
/**
 * 获取当前页面的相对路径
 * @returns 当前页面的相对路径（包含查询参数和哈希值）
 */
export declare const getOriginalUrl: (configs: SSOLoginConfig) => string;
/**
 * 获取登录链接
 * @param configs SSO登录配置
 * @returns 登录链接
 */
export declare const getLoginUrl: ({ configs, store, localPort, sdkVersion, customOriginalUrl, }: {
    configs: SSOLoginConfig;
    store: SSOStorage;
    localPort?: number;
    sdkVersion: string;
    customOriginalUrl?: string;
}) => string;
/**
 * 获取登出链接
 * @param configs SSO登录配置
 * @returns 登出链接
 */
export declare const getLogoutUrl: ({ configs, sdkVersion, }: {
    configs: SSOLoginConfig;
    sdkVersion: string;
}) => string;
/**
 * 创建Authorization Header值
 * @param token token值
 * @returns
 */
export declare const createAuthorizationHeader: (token?: string) => string;
/**
 * 获取一级权限的
 * @param env 环境变量
 * @param targetClientId 目标应用的clientId
 * @returns 申请链接地址
 */
export declare const getLevelOnePermissionApplyUrl: (accessEnv: SSOAccessEnvType, targetClientId: string) => string;
/**
 * 如果前一个相同任务处理结束（成功、失败），新任务会直接运行，并加入到任务缓存池中，
 * 否则会使用前一个任务的结果进行返回
 * @param taskTag 任务标记，用来区分任务是否相等
 * @param task 任务运行函数
 * @param lifeTime 新鲜度时间要求（单位：ms）
 * @returns 任务结果
 */
export declare const deduplicateTask: <R>(taskTag: string, task: () => Promise<R>, lifeTime?: number) => Promise<R>;
/**
 * 构建任务标记名
 * @param name 主名称
 * @param arg 参数
 */
export declare const buildTaskTagName: (name: string, ...args: unknown[]) => string;
