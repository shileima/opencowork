import { SSOLogLevel, SSOWorkModel } from '../config/const';
import type { SSOLogOutputInterface } from '../types';
interface LoggerOptions {
    packageName?: string;
    mode: SSOWorkModel;
    output?: SSOLogOutputInterface;
    logLevel?: SSOLogLevel;
}
declare class ConsoleOutput implements SSOLogOutputInterface {
    private mode;
    constructor(mode: SSOWorkModel);
    log(...args: unknown[]): void;
}
export default class SSOLogger {
    private mode;
    private output;
    private logLevel;
    private packageName?;
    constructor(options: LoggerOptions);
    info(tag: string, ...args: unknown[]): void;
    warn(tag: string, ...args: unknown[]): void;
    error(tag: string, ...args: Array<unknown | Error>): void;
    debug(tag: string, ...args: unknown[]): void;
    destroy(): void;
    updateLogLevel(logLevel: SSOLogLevel): void;
    private getLogPrefix;
}
export { ConsoleOutput, type LoggerOptions };
