"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCodeChallenge = exports.generateCodeVerifier = void 0;
exports.bufferToString = bufferToString;
var sha256_1 = __importDefault(require("crypto-js/sha256"));
var enc_base64url_1 = __importDefault(require("crypto-js/enc-base64url"));
var const_1 = require("../config/const");
var CHARSET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
function bufferToString(buffer) {
    var state = [];
    for (var i = 0; i < buffer.byteLength; i += 1) {
        var index = buffer[i] % CHARSET.length || 0;
        state.push(CHARSET[index]);
    }
    return state.join('');
}
/**
 * size in bytes
 */
var DEFAULT_SIZE = 64;
var HAS_CRYPTO = typeof window !== 'undefined' && !!window.crypto;
var cryptoGenerateRandom = function (sizeInBytes) {
    if (sizeInBytes === void 0) { sizeInBytes = DEFAULT_SIZE; }
    var buffer = new Uint8Array(sizeInBytes);
    if (HAS_CRYPTO) {
        window.crypto.getRandomValues(buffer);
    }
    else {
        // fall back to Math.random() if nothing else is available
        for (var i = 0; i < sizeInBytes; i += 1) {
            buffer[i] = Number(Math.random().toString().slice(2));
        }
    }
    return bufferToString(buffer);
};
var sha256Crypto = function (randomBytes) {
    return enc_base64url_1.default.stringify((0, sha256_1.default)(randomBytes));
};
/**
 * 生成Code verifier
 * @returns code verifier
 */
var generateCodeVerifier = function () {
    var randomBytes = cryptoGenerateRandom(32);
    return randomBytes;
};
exports.generateCodeVerifier = generateCodeVerifier;
/**
 * 生成Code challenge
 * @param codeVerifier code verifier
 * @returns
 */
var getCodeChallenge = function (codeVerifier) {
    if (const_1.OAUTH_PKCE_METHOD !== 'S256')
        return codeVerifier;
    return sha256Crypto(codeVerifier);
};
exports.getCodeChallenge = getCodeChallenge;
