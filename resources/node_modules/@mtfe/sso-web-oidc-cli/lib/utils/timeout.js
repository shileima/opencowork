"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.timeout = timeout;
var sso_web_oidc_base_1 = require("@mtfe/sso-web-oidc-base");
function timeout(fnPromise, ms) {
    var timer;
    var highOrderFetch = Promise.race([
        fnPromise,
        new Promise(function (resolve) {
            timer = setTimeout(function () {
                resolve((0, sso_web_oidc_base_1.buildResult)(sso_web_oidc_base_1.SSOErrorCode.TIMEOUT, null, 'timeout'));
            }, ms || 10 * 1000);
        }),
    ]);
    highOrderFetch.finally(function () {
        if (timer) {
            clearTimeout(timer);
        }
    });
    return highOrderFetch;
}
