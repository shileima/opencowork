import { type SSOLoginConfig } from '@mtfe/sso-web-oidc-base';
/**
 * Gets the configuration directory path
 * @returns The path to the configuration directory
 */
export declare function getConfigDir(configs: SSOLoginConfig): string;
/**
 * Ensures the configuration directory exists
 */
export declare function ensureConfigDir(configs: SSOLoginConfig): Promise<string>;
/**
 * Ensures the lock directory exists
 */
export declare function ensureLockDir(configs: SSOLoginConfig): Promise<string>;
