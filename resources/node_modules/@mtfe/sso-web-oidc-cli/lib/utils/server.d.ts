import { type SSOLogger, type SSOBaseClient, type SSOTokenInfoType, type SSOReturnType } from '@mtfe/sso-web-oidc-base';
export declare const waitForOAuthCallback: ({ logger, baseClient, timeout, abortSignal, }: {
    logger: SSOLogger;
    baseClient: SSOBaseClient;
    timeout?: number;
    abortSignal?: AbortSignal;
}) => Promise<SSOReturnType<SSOTokenInfoType | null>>;
