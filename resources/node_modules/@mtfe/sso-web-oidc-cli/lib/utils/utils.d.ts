import { type SSOLogger } from '@mtfe/sso-web-oidc-base';
export declare const sleep: (ms: number) => Promise<unknown>;
export declare const builtError: (error: unknown) => Record<"code", unknown>;
/**
 * 查找第一个可用端口
 * @param ports 端口数组，按优先级排序
 * @returns Promise<number | null> 第一个可用的端口号，如果都不可用则返回null
 */
export declare function findFirstAvailablePort(ports: number[]): Promise<number | null>;
/**
 * 重试函数，支持根据错误类型进行不同次数的重试
 * @param promiseFunction 要执行的Promise函数
 * @param logger 日志记录器
 * @param tag 日志标记，用于标识重试的操作
 * @returns Promise的返回值
 */
export declare function retryWithBackoff<T>(promiseFunction: () => Promise<T>, logger: SSOLogger, tag: string): Promise<T>;
