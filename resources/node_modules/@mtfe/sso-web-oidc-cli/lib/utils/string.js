"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringConcatenation = void 0;
/**
 * 字符串拼接函数
 * 将多个参数转换为字符串并拼接，自动过滤掉无效值
 *
 * @param args - 任意数量的参数，支持任何类型
 * @returns 拼接后的字符串
 */
var stringConcatenation = function () {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
    }
    // 存储有效的字符串项
    var itemList = [];
    // 遍历所有参数
    for (var _a = 0, args_1 = args; _a < args_1.length; _a++) {
        var arg = args_1[_a];
        // 过滤掉 null、undefined、空字符串
        if (arg !== null && arg !== undefined && arg !== '') {
            // 将有效参数转换为字符串并添加到列表中
            itemList.push(String(arg));
        }
    }
    // 将所有有效字符串拼接并返回
    return itemList.join('');
};
exports.stringConcatenation = stringConcatenation;
