export declare const getRandom: (min: number, max: number) => number;
/**
 * 随机睡眠一段时间
 * @param minMs 最小时间
 * @param maxMs 最大时间
 * @returns
 */
export declare const randomSleep: (minMs: number, maxMs: number) => Promise<unknown>;
